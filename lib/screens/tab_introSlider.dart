import 'package:flutter/material.dart';

class SliderIntroTab extends StatelessWidget {
  const SliderIntroTab({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Slidrr Intro'),
      ),
    );
  }
}