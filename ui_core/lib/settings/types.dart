enum UserType {user, anonymous}
enum MenuType { OnePage, TwoPage }
enum TableType { BoardTable, RollBandTable }
enum PermissionType{
  anonymousAccess,
  customerAccess,
  userManager,
  contentProducer,
  advancedSettings,
}